<Bindings>
	<Binding name="FISHING_MODE_TOGGLE" header="FISHING_MODE" category="ADDONS">
        if FishingModeFrame:IsShown() then
            FishingModeFrame:Hide()
        elseif InCombatLockdown() then
            print("Can't enable fishing mode during combat lockdown.")
        else
            FishingModeFrame:Show()
        end
	</Binding>
</Bindings>